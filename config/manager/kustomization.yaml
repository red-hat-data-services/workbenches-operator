# TODO: Add networkpolicy.yaml once the operator's target namespace is finalized.
# Ref: CWE-284 — no default-deny NetworkPolicy in place; cross-pod communication is unrestricted.
resources:
- manager.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
